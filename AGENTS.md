# Agents Instructions

## Python

- Use `uv` for all Python operations (running, testing, installing).
- Never invoke `python` or `python3` directly.

```bash
uv run pytest ...    # run tests
uv run mkdocs ...    # run mkdocs
uv pip install ...   # install packages
```
